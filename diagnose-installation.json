{
  "commands": [
    "echo '=== Moodle Installation Diagnostics ==='",
    "echo 'EFS Status:'",
    "df -h | grep efs || echo 'No EFS mounts'",
    "echo ''",
    "echo 'Service Status:'",
    "systemctl is-active httpd && echo 'Apache: Active' || echo 'Apache: Inactive'",
    "systemctl is-active php-fpm && echo 'PHP-FPM: Active' || echo 'PHP-FPM: Inactive'",
    "echo ''",
    "echo 'Moodle Files:'",
    "ls -la /app/moodle/index.php 2>/dev/null && echo 'Moodle index.php: EXISTS' || echo 'Moodle index.php: MISSING'",
    "ls -la /app/moodle/config.php 2>/dev/null && echo 'Moodle config.php: EXISTS' || echo 'Moodle config.php: MISSING'",
    "ls -la /app/moodle/health.php 2>/dev/null && echo 'Health endpoint: EXISTS' || echo 'Health endpoint: MISSING'",
    "echo ''",
    "echo 'Apache Configuration:'",
    "ls -la /etc/httpd/conf.d/moodle.conf 2>/dev/null && echo 'Moodle vhost: EXISTS' || echo 'Moodle vhost: MISSING'",
    "echo ''",
    "echo 'Testing Local Access:'",
    "curl -I http://localhost/ 2>/dev/null | head -1 || echo 'Local access failed'",
    "curl -I http://localhost/health.php 2>/dev/null | head -1 || echo 'Health endpoint failed'",
    "curl -I http://localhost/login/index.php 2>/dev/null | head -1 || echo 'Login page failed'",
    "echo ''",
    "echo 'Apache Error Log (last 10 lines):'",
    "tail -10 /var/log/httpd/error_log 2>/dev/null || echo 'No Apache error log'",
    "echo ''",
    "echo 'Moodle Error Log (last 5 lines):'",
    "tail -5 /var/log/httpd/moodle_error.log 2>/dev/null || echo 'No Moodle error log'",
    "echo ''",
    "echo 'Directory Permissions:'",
    "ls -la /app/ | head -5",
    "ls -la /data/ | head -5"
  ]
}
