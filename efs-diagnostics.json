{
  "commands": [
    "set -euo pipefail",
    "echo '=== EFS Diagnostics ==='",
    "TOKEN=$(curl -sS -X PUT http://169.254.169.254/latest/api/token -H X-aws-ec2-metadata-token-ttl-seconds:21600 || true)",
    "REGION=$(curl -sS -H \"X-aws-ec2-metadata-token: $TOKEN\" http://169.254.169.254/latest/meta-data/placement/region || echo ca-central-1)",
    "IID=$(curl -sS -H \"X-aws-ec2-metadata-token: $TOKEN\" http://169.254.169.254/latest/meta-data/instance-id)",
    "echo Region: $REGION Instance: $IID",
    "echo '--- Instance networking ---'",
    "aws ec2 describe-instances --instance-ids $IID --region $REGION --query 'Reservations[0].Instances[0].[SubnetId,SecurityGroups[*].GroupId,Placement.AvailabilityZone,PrivateIpAddress]' --output table || true",
    "SUBNET=$(aws ec2 describe-instances --instance-ids $IID --region $REGION --query 'Reservations[0].Instances[0].SubnetId' --output text || echo '')",
    "echo Subnet: $SUBNET",
    "echo '--- EFS mount targets (Data) ---'",
    "aws efs describe-mount-targets --file-system-id fs-075a2be536c08840c --region $REGION --query 'MountTargets[*].[MountTargetId,SubnetId,LifeCycleState,AvailabilityZoneName,NetworkInterfaceId]' --output table || true",
    "echo '--- EFS mount targets (App) ---'",
    "aws efs describe-mount-targets --file-system-id fs-0c60c5879a0dcecb1 --region $REGION --query 'MountTargets[*].[MountTargetId,SubnetId,LifeCycleState,AvailabilityZoneName,NetworkInterfaceId]' --output table || true",
    "echo '--- EFS SGs ---'",
    "for FS in fs-075a2be536c08840c fs-0c60c5879a0dcecb1; do MT=$(aws efs describe-mount-targets --file-system-id $FS --region $REGION --query 'MountTargets[0].MountTargetId' --output text || echo ''); echo FS:$FS MT:$MT; aws efs describe-mount-target-security-groups --mount-target-id $MT --region $REGION --query 'SecurityGroups' --output text || true; done",
    "echo '--- SG rules (Instance SG) ---'",
    "ISG=$(aws ec2 describe-instances --instance-ids $IID --region $REGION --query 'Reservations[0].Instances[0].SecurityGroups[0].GroupId' --output text || echo '')",
    "aws ec2 describe-security-groups --group-ids $ISG --region $REGION --query 'SecurityGroups[0].[GroupId,IpPermissions[?ToPort==`2049`]]' --output json || true",
    "echo '--- NACLs on instance subnet ---'",
    "aws ec2 describe-network-acls --filters Name=association.subnet-id,Values=$SUBNET --region $REGION --query 'NetworkAcls[*].[Associations[0].SubnetId,Entries]' --output json || true",
    "echo '--- DNS and port check ---'",
    "nslookup fs-075a2be536c08840c.efs.$REGION.amazonaws.com || true",
    "nslookup fs-0c60c5879a0dcecb1.efs.$REGION.amazonaws.com || true",
    "command -v nc >/dev/null 2>&1 || dnf install -y nmap-ncat >/dev/null 2>&1 || true",
    "nc -zv fs-075a2be536c08840c.efs.$REGION.amazonaws.com 2049 >/dev/null 2>&1 || echo 'Data EFS port 2049 not reachable'",
    "nc -zv fs-0c60c5879a0dcecb1.efs.$REGION.amazonaws.com 2049 >/dev/null 2>&1 || echo 'App EFS port 2049 not reachable'",
    "echo '--- Current mounts ---'",
    "df -h | sed -n '1,200p'",
    "echo '=== EFS Diagnostics Complete ==='"
  ]
}
