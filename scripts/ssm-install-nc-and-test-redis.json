{
  "commands": [
    "sudo yum -y install -q nmap-ncat || sudo dnf -y install -q nmap-ncat || true",
    "nc -h >/dev/null 2>&1 && echo NC_INSTALLED || echo NC_MISSING",
    "HOST=shared-redis-ha.kbi0dr.ng.0001.cac1.cache.amazonaws.com",
    "echo HOST=$HOST",
    "getent hosts $HOST || true",
    "echo TEST TCP 6379",
    "nc -z -w2 $HOST 6379 >/dev/null 2>&1 && echo OK || echo FAIL"
  ]
}

