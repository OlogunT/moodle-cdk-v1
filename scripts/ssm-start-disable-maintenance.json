{
  "commands": [
    "set -e",
    "echo 'Start services and disable maintenance'",
    "systemctl start php-fpm || systemctl start php-fpm8.1 || systemctl start php-fpm8.0 || true",
    "systemctl start httpd || true",
    "cd /app/moodle",
    "if [ -x /usr/bin/php ]; then PHP=/usr/bin/php; elif [ -x /usr/bin/php80 ]; then PHP=/usr/bin/php80; else PHP=php; fi",
    "sudo -u apache $PHP admin/cli/maintenance.php --disable || true",
    "echo 'Services started and maintenance disabled'"
  ]
}

